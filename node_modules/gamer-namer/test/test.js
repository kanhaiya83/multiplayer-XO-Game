"use strict";

var expect = require("chai").expect;
var gamerNamer = require("../index");

describe("#Name Generation ", function() {
  it("should generate name", function() {
    var result = gamerNamer.generateName();
    expect(result).to.not.equal(null);
  });
});
